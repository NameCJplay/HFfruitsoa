server:
  port: 8088
spring:
  application:
    name: eureka
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      defaultZone: http://127.0.0.1:${server.port}/eureka/
ribbon:
  eureka:
    enabled: true