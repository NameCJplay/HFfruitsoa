server:
  port: 8090
spring:
  application:
    name: domain
#  main:
#    web-application-type: none
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8088/eureka/
hystrix:
  metrics:
    enabled: true
ribbon:
  eureka:
    enabled: true

